use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, Serialize, Deserialize)]
pub struct ItemEvidenceContent {
    pub item_id: u64,
    pub item: String,
    pub tier: u64,
    pub cost: u64,
    pub slot: String,
    pub active: bool,
    pub adopter_matches: u64,
    pub eligible_player_matches: u64,
    pub purchase_events: u64,
    pub wins: u64,
    pub adoption: f64,
    pub observed_outcome_rate: f64,
    pub median_buy_time_s: f64,
    pub median_valid_buy_net_worth: Option<f64>,
    pub buy_net_worth_q25: Option<f64>,
    pub buy_net_worth_q75: Option<f64>,
    pub valid_buy_net_worth_share: f64,
    pub selection_adopter_matches: u64,
    pub selection_eligible_player_matches: u64,
    pub training_adopter_matches: u64,
    pub training_eligible_player_matches: u64,
    pub validation_adopter_matches: u64,
    pub validation_eligible_player_matches: u64,
    pub test_adopter_matches: u64,
    pub test_eligible_player_matches: u64,
    pub selection_adoption: f64,
    pub training_adoption: f64,
    pub validation_adoption: f64,
    pub test_adoption: f64,
    pub selection_median_buy_time_s: Option<f64>,
    pub selection_median_valid_buy_net_worth: Option<f64>,
    pub selection_buy_net_worth_q25: Option<f64>,
    pub selection_buy_net_worth_q75: Option<f64>,
    pub selection_valid_buy_net_worth_share: f64,
    pub selection_valid_buy_net_worth_observations: u64,
    pub training_valid_buy_net_worth_observations: u64,
    pub validation_valid_buy_net_worth_observations: u64,
    pub training_buy_net_worth_q25: Option<f64>,
    pub training_buy_net_worth_q75: Option<f64>,
    pub validation_buy_net_worth_q25: Option<f64>,
    pub validation_buy_net_worth_q75: Option<f64>,
    pub imbue_target_ability_id: Option<u64>,
    pub imbue_target_ability: Option<String>,
    pub imbue_target_matches: u64,
    pub imbue_observations: u64,
    pub imbue_target_share: f64,
}
