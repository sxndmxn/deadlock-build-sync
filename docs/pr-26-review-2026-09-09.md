Complete this task: Correct source identity and artifact consistency failures in PR #26.

Required output: A local branch with three corrections, regression tests, and verified quality-gate results.

Success criteria:

1. Resume rejects changed manifests, source databases, assets, and incompatible discovery checkpoints before validation.
2. Compatible resume preserves the candidate family, guide groups, defaults, and source files.
3. Build and sync bundles contain the exact admitted evidence bytes, including evidence supplied through an external path.
4. Failed generation or replacement preserves the previous bundle.
5. Status reports current for correctly installed guide groups.
6. Status rejects missing groups, extra variants, and incorrect snapshot or policy identities.
7. The complete fast local gate passes without weaker limits or exclusions.

Constraints: Preserve Steam data and existing user files. Keep the statistical methods, support limits, and purchase guides unchanged.
Use the Python and uv workflow. Do not run a live Steam sync.

Relevant context: PR #26 uses branch `feat/build-purchase-guidance` at `a35627d21aa97943a6df80550b1230de20880f92`.
The existing suite passed with 1,255 tests.
Linux CI and CodeQL passed on that commit.
PR #26 contains the commit from the older open PR #23.

Verified failures:

- Resume accepted modified `manifest.json`, `analysis.duckdb`, and `items-all.json` files after discovery.
  The candidate fingerprint did not identify the source files.
- Build returned success without `build-evidence.json` in the output bundle.
  The staging function copied the output directory but did not write the admitted evidence bytes.
- Status reported stale for one correctly installed guide that contained two variants.
  It required a separate installed entry for each variant in the context.

The initial regression run produced six expected failures and four passing rejection cases.
The failures identify the missing behavior; the existing rejection cases remain required.

The first status correction treated an empty group ID as a separate variant.
The fixture exposed this error. The correction now uses the same default-ID rule as guide generation.
The missing-group test still requires a stale result.

The numeric gate rejected the expanded build test because its cyclomatic complexity was 24.
Output checks now use a separate test helper. The complexity limit remains 21.

The source checkpoint now records file hashes and a method version.
Checks run before resume and after validation. Source changes during discovery prevent checkpoint publication.
Older checkpoints lack this proof and require a new `--run-id`.

Build and sync stage the admitted evidence bytes with the other bundle files.
Later changes to an external evidence file cannot change the staged evidence.
The existing guide-output fingerprint remains unchanged.

Review method: Read the PR description, checks, changed code, repository instructions, and relevant tests.
Use local fixtures to reproduce failures before changes.
`gh pr diff --stat` was unavailable. `git diff --stat` supplied the local change summary.

Verification results: All success criteria passed on local branch `fix/artifact-source-consistency`.

| Check | Result |
| --- | --- |
| Complete test suite with warnings as errors | 1,287 passed; 14.38 seconds |
| Statement coverage | 97.10% |
| Branch coverage | 91.71% |
| Ruff formatting and lint, strict ty | Passed |
| Deptry and Tach | Passed |
| Complexipy and numeric quality gate | Passed |
| Vulture and Pylint duplicate-code check | Passed |
| uv lock, frozen environment, and dependency checks | Passed |
| Source distribution and wheel build | Passed |
| Wheel smoke tests outside the checkout | Both CLI help commands passed with runtime dependencies only |
| Wheel source inspection | All four changed product files matched the checkout |
| Existing guide-output fingerprint | Unchanged |
| Failed bundle generation and replacement | Previous bundle preserved |

The source tests cover changed or missing manifests, databases, and each consumed asset file.
They also cover incompatible checkpoint versions and source changes during discovery or validation.
The compatible resume test preserves the saved candidates, groups, family size, and source data.

The bundle tests cover new and existing evidence files, external paths, and source changes after admission.
The status tests cover both explicit and empty default group IDs.
Missing groups, extra variants, incorrect snapshots, and incorrect policies remain stale.

No quality limit, dependency, schema for installed builds, or statistical method changed.
The complete fast local gate passed. The full mutation gate did not run; its Steam write modules are unchanged.

Remaining checks: Live Steam installation and actual screen fit require a separate client check.
No live Steam sync or new full-roster statistical analysis ran.
The earlier Linux CI result applies to the reviewed PR commit, not this local commit.
The branch has not been pushed.
